#include <stdio.h>
int main()
{
	int n,i,f;
	scanf("%d",&n);
	for(i=1;i<n;i++)
	{
		if(n%i==0)
		{
		f=i;
		}}
		
		if(f>1)
		{
			printf("the number is composite");
		}
		else
		{
			printf("the number is not a composite");
		}
	return  0;
}

